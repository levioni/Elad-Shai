 
public interface filter {
	/**
	 * this is the interface i used to filter my list
	 * @param s for the String you want to filter (time, location or device ID)
	 * @return
	 */
	
	public boolean test(String s);

}
